1.service层（1.状态处理转义（消除if else） 2.集合Stream(新特性) 3.optionl判空）
 1.1 消除if else结果模板：
 public void add(Object element) {
 if (readOnly) {
   return;
 }

 if (overCapacity()) {
   grow();
 }

 addElement(element);
}
