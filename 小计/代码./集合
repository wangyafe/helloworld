1.集合用流
2. for循环代替：.stream().forEach(recording->{
                    if(recording !=null){
                        resp.setFileName(recording.getFileName());
                    }});
   2.1 并行流 cronJobBoList.parallelStream().forEach(this::doOne);
3.遍历删除用倒序 list遍历删除元素的正确做法 https://blog.csdn.net/qq_40256408/article/details/105839409?share_token=c3a84c76-354e-4424-831e-d2dd35952088
4.改变集合中的元素类型 使用map  4.1 optionl判空修改属性 Optional.ofNullable(applyList).ifPresent(vo::setApplyList);
5.分割字符串创建流：Arrays.stream(batchMeetingSummary.getMeetingSummaryIds().split(","))
                            .map(meetingSummaryId -> {
                                MeetingSummaryQueryCond cond = new MeetingSummaryQueryCond();
                                cond.setId(meetingSummaryId);
                                cond.setTntInstId(tntInstId);
                                List<MeetingSummary> list = meetingSummaryService.queryByCondition(cond);
                                return CollectionUtils.isEmpty(list) ? null : list.get(0);
                            })
6.java List＜Bean＞ 根据某个属性值获取到该对象 https://blog.csdn.net/Romantic_321/article/details/123568689
 6.1 list.stream().filter(user -> Objects.equals(user.getId(),1)).findFirst();
7.Java8 stream特性之一：List转Map方案（返回某个属性或对象本身）https://blog.csdn.net/qq_41732361/article/details/113932446
 7.1  Map<Integer,User> userMap3 = userList.stream().collect(Collectors.toMap(User::getId, Function.identity()));
