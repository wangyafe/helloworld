1.破坏双亲委派的自定义类加载器
  1.1 重写loadClass()方法
  1.2 TOMCAT:每一个应用自己的类加载器——WebAppClassLoader负责加载本身的目录下的class文件，加载不到时再交给CommonClassLoader加载，这和双亲委派刚好相反
2.不破坏双亲委派的自定义类加载器
  1.1 只需要重写findClass()方法
  1.2 作用：1.加密（防止反编译） 2.从非标准的来源加载代码
