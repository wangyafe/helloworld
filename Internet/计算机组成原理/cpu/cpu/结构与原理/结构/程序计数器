1.cpu中的一个寄存器 目标：存放指令地址
2.在 CPU 执行指令的过程中，程序计数器会自动递增，指向下一条指令的地址。当 CPU 完成当前指令的执行后，它会从程序计数器中获取下一条指令的地址，并将其加载到指令寄存器中，以便进行解码和执行。
程序计数器的作用是确保 CPU 能够按照程序的顺序执行指令。它在指令流控制中起着关键作用，帮助 CPU 正确地执行程序。
需要注意的是，不同的 CPU 架构可能会有不同的实现方式，但程序计数器通常都是 CPU 内部的一个重要组成部分