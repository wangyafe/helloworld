1.超频 2.降频
3.调整 CPU 的时钟信号通常是由 CPU 内部的硬件和固件来控制的，具体的实现方式可能因不同的 CPU 架构和设计而有所不同。以下是一些常见的方法：
 1. PLL（Phase-Locked Loop）调整：PLL 是一种用于生成时钟信号的电路，它可以根据输入的参考信号调整输出时钟的频率。通过改变 PLL 的参数，如分频比或倍频比，可以实现时钟频率的调整。
4.场景：1. 电源管理单元：现代的 CPU 通常集成了电源管理单元，它可以根据系统的负载和功耗需求来调整时钟频率。电源管理单元会监测系统的活动，并根据需要降低或提高时钟频率，以达到节能的目的。
  2. BIOS 设置：在计算机的 BIOS（Basic Input/Output System）中，可能提供了一些选项来手动调整 CPU 的时钟频率。用户可以进入 BIOS 设置界面，找到相关的选项来设置CPU 的频率。
  3. 操作系统电源管理：操作系统也可以参与 CPU 时钟频率的调整。例如，在 Windows 操作系统中，可以通过电源选项来设置不同的电源模式，例如平衡模式、节能模式或高性能模式，这些模式会影响 CPU 的时钟频率。
   3.1  需要注意的是，对于普通用户来说，直接调整 CPU 时钟频率可能需要一定的技术知识和经验，并且不当的调整可能会导致系统不稳定或甚至损坏硬件。因此，一般情况下，建议使用系统提供的自动频率调整功能或预设的电源管理选项，以确保系统的稳定性和安全性