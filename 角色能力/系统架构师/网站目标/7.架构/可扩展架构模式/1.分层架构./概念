1.展示层 → 业务层 → 数据层 → 存储层（流程拆分 仍在一个进程内）
 1.1 分层结构的代价就是冗余 1.2 按照分层分册架构来实现，每层都要写一个简单的函数
 1.3 分层架构的优势就体现在通过分层强制约束两两依赖，一旦自由选择绕过分层，时间一长，架构就会变得混乱
2. 57可扩展架构模式：分层架构和SOA https://www.toutiao.com/article/7145290607869575692/?log_from=3fae32621410e_1664113800209
