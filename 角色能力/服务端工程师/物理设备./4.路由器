1.网络层（数据包+源目mac+源目IP）
  0.网络层由来：发送给外网的数据包，如何统统先发送给路由器（如果只有mac地址 交换机广播给所有的设备 查找地址）
  1.路由表 用于映射 IP 地址(段)和它的端口
  2.arp缓存表 用于缓存 IP 和 MAC 地址的映射关系
  3.转化表 用于记录私网IP+端口和公网IP+端口的映射关系
  4.建立过程：1.arp 缓存表是不断通过 arp 协议的请求逐步完善起来的（接入时广播消息） 2.路由表是各种路由算法 + 人工配置逐步完善起来的
1.路由器的每一个端口，都有独立的 MAC 地址（有ip和mac） https://www.cnblogs.com/flashsun/p/14266148.html
2.静态路由-路由负载均衡（路由器(任何一台设备)可以有负载功能） https://blog.51cto.com/lxlaizz/3717443 https://www.cxyzjd.com/article/monologuezjp/104773277
  2.1动态路由会根据路由策略更新路由表（1.满载2.断点）
  2.2（数据包传递路径不唯一）路由器需要根据一定的选择规则，如距离、速度、费用等，选择最合适的路径，实现数据包的快速传输
  3.3举个例子，如果数据包从A节点要传输到D节点，而在网络中存在两条路径，一条是从A到B再到D，另一条是从A到C再到D，那么路由器可以根据拥塞状态、带宽、时延等信息
  3.4选择最优的路径进行数据包的传输。但是在数据包传输过程中，网络中的拥塞情况可能会发生变化，这时候路由器会根据新的网络拓扑和拥塞情况，重新选择传输路径。总之，分组交换网络中的数据包可以采用不同的传输路径，但具体应用取决于网络的拓扑结构和路由算法。多路径传输可以提高网络的带宽利用率，并减少数据丢失率
3.图文并茂详解 NAT 协议！ https://juejin.cn/post/7067706621220093965
4.有公网ip和私网ip,那么是怎么找到一个具体的主机的？（公用公网IP时 用端口区别内网IP） https://www.zhihu.com/question/56576553
